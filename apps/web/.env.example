# API URL - Leave empty for development (uses Vite proxy)
# For production, set to your API server URL (e.g., https://api.invoice.sayedmowafi.dev)
VITE_API_URL=

# Cold Start Screen - Shows a loading screen while waiting for backend to wake up
# Only needed for free-tier hosting (Render, etc.) that sleeps after inactivity
# Set to 'true' to enable, leave empty or 'false' to disable (default: disabled)
VITE_COLD_START_SCREEN=
