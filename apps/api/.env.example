# Server Configuration
PORT=3001
NODE_ENV=development

# CORS - Frontend URL
CORS_ORIGIN=http://localhost:5173

# ===========================================
# Rate Limiting (disabled by default)
# ===========================================

# Enable/disable rate limiting (set to 'true' to enable)
RATE_LIMIT_ENABLED=false

# General API rate limit
RATE_LIMIT_WINDOW_MS=900000          # 15 minutes (in milliseconds)
RATE_LIMIT_MAX_REQUESTS=100          # Max requests per window

# File upload rate limit (stricter)
RATE_LIMIT_UPLOAD_WINDOW_MS=3600000  # 1 hour (in milliseconds)
RATE_LIMIT_UPLOAD_MAX=10             # Max uploads per window

# PDF generation rate limit (strictest - resource intensive)
RATE_LIMIT_GENERATE_WINDOW_MS=3600000  # 1 hour (in milliseconds)
RATE_LIMIT_GENERATE_MAX=5              # Max generations per window
