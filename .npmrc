# Skip Puppeteer's Chromium download during npm install
# This is needed for Cloudflare Pages which only builds the frontend
# The API (which uses Puppeteer) runs on Render with Docker where Chromium is installed separately
puppeteer_skip_download=true

